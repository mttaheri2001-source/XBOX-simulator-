{
  "name": "xbox-s-series-android-simulator",
  "version": "0.2.0",
  "description": "Xbox Series S inspired dashboard shell with online downloads and Android runtime bridge.",
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "web": "node server.js",
    "check": "node --check electron-main.js && node --check preload.js && node --check server.js && node --check app.js"
  },
  "devDependencies": {
    "electron": "^38.1.0"
  }
}
